export class Customer {
    constructor(input) {
        this.id = input.id;
        this.name = input.name;
        this.x = input.coordinates.x;
        this.y = input.coordinates.y;
    }

}